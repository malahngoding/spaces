export interface InsteadLocale {
  locale: string;
  title: string;
  subtitle: string;
  profile: {
    button: string;
  };
  welcome: string;
  cookies: {
    firstConsent: string;
    secondConsent: string;
    acceptConsent: string;
    declineConsent: string;
  };
  footer: {
    rights: string;
  };
}
