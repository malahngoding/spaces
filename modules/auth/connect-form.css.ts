import { sprinkles } from '@styles/sprinkles.css';
import { style } from '@vanilla-extract/css';

export const styledConnectFormWrapper = style([
  sprinkles({ display: `flex`, marginY: `large` }),
]);
