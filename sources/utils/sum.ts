export default function sum(a: number, b: number): number {
  return a + b;
}

function mantap() {
  return 'pancing';
}

type Person = {
  name: string;
  age: number;
  isOld: boolean;
};

function mantapTS(): Person {
  return {
    name: 'pancing',
    age: 28,
    isOld: true,
  };
}
